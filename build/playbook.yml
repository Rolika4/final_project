---
- name: Configure build server, build .jar, create container, push container
  hosts: all
  become: yes

  tasks:
  - name: Create file with variable
    shell: |
      touch my_first_file